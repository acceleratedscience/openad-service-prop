apiVersion: v1
kind: Service
metadata:
  name: {{ include "property-inference-service-chart.fullname" . }}
  labels:
    {{- include "property-inference-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "property-inference-service-chart.selectorLabels" . | nindent 4 }}
